import type { ChartFontStyle } from "./Chart.types";

/**
 * defaultChartFont
 * ------------------------------------------------------------
 * Fonte padrão dos gráficos
 */
export const defaultChartFont: ChartFontStyle = {
  family: "Lato, sans-serif",
  sizeDesktop: 16,
  sizeMobile: 14,
  color: "inherit", // vem do tema
  weight: 600,
};
